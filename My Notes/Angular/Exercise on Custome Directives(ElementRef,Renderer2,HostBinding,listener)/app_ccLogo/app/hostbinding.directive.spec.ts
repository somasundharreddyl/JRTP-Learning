import { HostbindingDirective } from './hostbinding.directive';

describe('HostbindingDirective', () => {
  it('should create an instance', () => {
    const directive = new HostbindingDirective();
    expect(directive).toBeTruthy();
  });
});
