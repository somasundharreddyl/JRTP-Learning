import { RendererDirective } from './renderer.directive';

describe('RendererDirective', () => {
  it('should create an instance', () => {
    const directive = new RendererDirective();
    expect(directive).toBeTruthy();
  });
});
